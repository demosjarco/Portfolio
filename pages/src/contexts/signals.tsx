import { Focusing, PowerState, type IconStates } from './types';

export const defaultFocus: Focusing = Focusing.window;
export const defaultIconState: IconStates = [];
export const defaultState: PowerState = PowerState.start;
